import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  texto: {
    fontSize: 30,
  },
  info: {
    marginTop: 200,
    alignItems: "center",
  },
  button: {
    backgroundColor: "red",
  },
});
